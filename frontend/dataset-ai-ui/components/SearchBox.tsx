// Member 3 — TODO: search input that submits a natural-language query to
// services/api.ts and renders the result (loading/error states included).

export default function SearchBox() {
  return null;
}
