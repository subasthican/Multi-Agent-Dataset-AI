// Member 3 — TODO: typed client for the FastAPI gateway.
// Read the base URL from an env var (e.g. NEXT_PUBLIC_API_BASE_URL),
// do not hardcode "http://localhost:8000".
//
// export async function searchDataset(query: string) { ... POST /nlp-agent ... }
