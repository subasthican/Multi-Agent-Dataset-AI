# Member 2 — TODO: field-level encryption helpers (e.g. via `cryptography`'s
# Fernet). Load the key from an environment variable — never hardcode it.
#
#   def encrypt_data(data: str) -> bytes: ...
#   def decrypt_data(token: bytes) -> str: ...
