# Member 2 — TODO: authentication dependency for FastAPI routes
# (e.g. a `get_current_user` dependency that validates the JWT from
# jwt_manager.py and raises HTTPException(401) when invalid/missing).
